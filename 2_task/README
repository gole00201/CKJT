Помимо конвертации XML в JSON файл, решил попробовать проверить скорость чтения данных из этих фалов. 

Результаты следующие:
1. Время чтения из джейсон файла в два раза меньше, чем при чтении из XML файла.
(test_json.py выполнился в два раза быстрее чем test_xml.py)
2. Размер джейсон файла в два раза больше чем размер XML файла. Но стоит учитывать тот факт, что джейсон файл
является значительно читабельнее. 